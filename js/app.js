/* ========================================
   APP — math·logic
   Точка входа: инициализация, глобальные события
   ======================================== */

document.addEventListener('DOMContentLoaded', function() {
  NAV.fixLinks();
  UI.initAnimations();
  ML.applySettings();
  ML.updateLastVisit();
});
